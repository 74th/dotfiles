{
  "NodeJS ファイル書き込み": {
    "prefix": "Nodejs ファイル書き込み",
    "body": ["var fs = require(\"fs\");", "fs.writeFile(\"$1\", $2);"],
    "description": "ファイルを書き込む"
  },
  "NodeJS ファイル読み込み": {
    "prefix": "Nodejs ファイル読み込み",
    "body": [
      "var fs = require(\"fs\");",
      "fs.readFile(\"$1\", function(err, text) {",
      "    text.toString();",
      "});"
    ],
    "description": "ファイル読み込み"
  }
}
