#!/bin/bash

usage() {
	cat <<'EOF'
Usage: compress-targz <output.tar.gz> <files/directories...>

Create a gzip-compressed tar archive.
EOF
}

case "${1:-}" in
	""|-h|--help)
		usage
		exit 0
		;;
esac

set -xe
tar -zcvf "$@"
